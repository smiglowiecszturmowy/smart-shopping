<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:SmartShoppingXamarin"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance rootViewModel:MainPageViewModel}"
             x:Class="SmartShoppingXamarin.View.MainPage">
    
    <StackLayout>
        <ListView ItemsSource="{Binding Path=HomeIngredients}"></ListView>
        <Entry x:Name="Entry" Placeholder="Ingredient"></Entry>
        <Button Command="{Binding Path=AddCommand}" CommandParameter="{x:Reference Entry}" />
    </StackLayout>

</ContentPage>