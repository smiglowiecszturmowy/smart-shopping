<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             BindingContext="{Binding Source={RelativeSource Self}, Path=ViewModel}"
             x:Class="SmartShoppingXamarin.View.MainPage">
    
    <StackLayout>
        <ListView ItemsSource="{Binding Path=HomeIngredients}"></ListView>
        <Button Command="{Binding Path=AddCommand}" Text="Dodaj produkt" Padding="10" />
    </StackLayout>

</ContentPage>